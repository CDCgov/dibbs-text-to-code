[project]
name = "text-to-code"
version = "0.1.0"
readme = "README.md"
dependencies = [
  "lxml>=6.0.2",
  "pydantic>=2.12.5",
  "sentence-transformers>=5.3.0,<5.4.0", # sentence-tranformers introduces a breaking change at 5.4.0 that we would want to more carefully evaluate before upgrading to it
  "torch",
]

[dependency-groups]
dev = ["types-lxml>=2026.2.16"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/text_to_code"]
