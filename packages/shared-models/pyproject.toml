[project]
name = "shared-models"
version = "0.1.0"
description = "Shared pydantic models and code-system constants used across the TTC pipeline"
readme = "README.md"
authors = [{ name = "Josh Nygaard", email = "jnygaard@skylight.digital" }]
dependencies = []

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"
