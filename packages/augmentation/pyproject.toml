[project]
name = "augmentation"
version = "0.1.0"
readme = "README.md"
dependencies = [
    "lxml>=4.9.2",
    "pydantic>=2.12.3"
]


[dependency-groups]
dev = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/augmentation"]