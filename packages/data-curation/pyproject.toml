[project]
name = "data-curation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Josh Nygaard", email = "josh.nygaard@gmail.com" }
]
dependencies = [
  "requests",
  "python-dotenv"
]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
