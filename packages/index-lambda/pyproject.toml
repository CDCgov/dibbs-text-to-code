[project]
name = "index-lambda"
version = "0.1.0"
readme = "README.md"
dependencies = ["boto3", "opensearch-py", "requests_aws4auth"]

[dependency-groups]
dev = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/index_lambda"]