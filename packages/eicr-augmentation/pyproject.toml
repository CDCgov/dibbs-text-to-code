[project]
name = "eicr-augmentation"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.11, <3.13"
dependencies = [    
]

[dependency-groups]
dev = [
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
