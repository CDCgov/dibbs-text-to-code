[project]
name = "utils"
version = "0.1.0"
description = "Environment-variable and LOINC text-processing helpers shared across packages"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/utils"]
