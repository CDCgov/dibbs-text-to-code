[project]
name = "validation"
version = "0.1.0"
readme = "README.md"
dependencies = ["saxonche>=12.9.0", "shared-models"]

[tool.uv.sources]
shared-models = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/validation"]
