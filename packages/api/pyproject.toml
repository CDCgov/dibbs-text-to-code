[project]
name = "api"
version = "0.1.0"
description = ""
dependencies = [
    "fastapi",
    "opensearch-py==3.0.0"
]

[dependency-groups]
dev = [
  "fastapi[standard]"
]

[tool.ruff]
extend = "../../pyproject.toml"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
